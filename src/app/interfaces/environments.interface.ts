export interface environmentInterface
{
    production: boolean;
    log: boolean;
    lang: string;
    apiUrl: string;
    grant_type: 'password';
    client_id: string;
    client_secret: string;
}
